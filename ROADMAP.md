# 仍需完成的差距

0.19.0 已具原始输入、字符串、异常和可用的持续会话，整体“追平”仍未完成。

1. 完整标准词集与编译语义：控制流栈、多 WHILE、CASE、更多 POSTPONE 编译词、搜索顺序/wordlists、词典查询、词重定义边界和完整标准测试。
2. 数值系统：双 cell、浮点、>NUMBER 与 pictured numeric output、完整错误码及平台 cell 模型。
3. 输入输出与宿主：KEY/ACCEPT、终端输入 SOURCE/REFILL、INCLUDE/文件词、通用堆、更多宿主接口与持久化会话。当前 feed 支持跨行定义，但字符串/注释必须在单个输入片段内闭合。
4. 兼容性：更多 Gforth 版本、Unicode/字节/非标准扩展、全部模糊或实现定义行为；当前 128 案例排除了地址和 32/64 位边界。
5. 性能及运行质量：代表性相同任务对照、峰值内存、长时间运行、更多平台、完整上游套件。当前短样本和 1000 次 Worker 往返不能证明性能追平。

所有开发和证据均保留在本地。远程 CI、上传、发布、提交参赛材料及替换命名空间尚未进行；旧 ZIP/bundle 为历史快照。
