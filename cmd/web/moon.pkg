import {
  "mawei2018/forth" @lib,
  "moonbitlang/core/json",
}

supported_targets = "js"

pkgtype(kind: "executable")

options(
  link: { "js": { "exports": [ "run", "report", "session" ], "format": "esm" } },
)
